server:
  port: 9090

logging:
  level:
    org:
      springframework: trace

spring:
  ldap:
    embedded:
      port: 8389
      ldif: classpath:test-server.ldif
      base-dn: dc=springframework,dc=org